name: Ship GCP

on:
  push:
    branches: [ main,cd ]
  workflow_dispatch:

permissions:
  contents: read
  id-token: write  # Required for Workload Identity Federation

jobs:
  detect-changes:
    runs-on: ubuntu-latest
    outputs:
      matrix: ${{ steps.make-matrix.outputs.matrix }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Show changed files
        run: |
            echo "HEAD: ${{ github.sha }}"
            echo "BEFORE: ${{ github.event.before }}"
            git --no-pager log --oneline -n 3
            echo "---- changed files in this push range ----"
            git --no-pager diff --name-only ${{ github.event.before }} ${{ github.sha }} || true
            echo "------------------------------------------"

      - name: Filter changed paths
        id: changes
        uses: dorny/paths-filter@v3
        with:
          base: ${{ github.event.before }}
          ref:  ${{ github.sha }}
          filters: |
            # rebuild all 3 if these change:
            global:
              - 'pom.xml'
              - 'docker/**'
              
            frontend:
              - added|deleted|modified: 'frontend/**'
            inventory-service:
              - added|deleted|modified: 'inventory-service/**'
            order-service:
              - added|deleted|modified: 'order-service/**'


      - name: Build dynamic matrix
        id: make-matrix
        shell: bash
        run: |
          echo '${{ toJSON(steps.changes.outputs) }}'
          set -e

          # If any "global" files changed, rebuild everything.
          if [[ "${{ steps.changes.outputs.global }}" == "true" ]]; then
            modules=(
              frontend
              inventory-service
              order-service
            )
          else
            modules=()
            [[ "${{ steps.changes.outputs.frontend }}"       == "true" ]] && modules+=(frontend)
            [[ "${{ steps.changes.outputs.inventory-service }}" == "true" ]] && modules+=(inventory-service)
            [[ "${{ steps.changes.outputs.order-service }}"      == "true" ]] && modules+=(order-service)
                
          fi

          # Build the matrix JSON that GH Actions expects: { "include": [ {module, image_name}, ... ] }
          if (( ${#modules[@]} == 0 )); then
            echo 'matrix={"include":[]}' >> "$GITHUB_OUTPUT"
            exit 0
          fi

          if (( ${#modules[@]} == 0 )); then
            echo 'matrix={"include":[{"module":"NONE","image_name":"NONE"}]}' >> "$GITHUB_OUTPUT"
            exit 0
          fi

          json='{"include":['
          sep=""
          for m in "${modules[@]}"; do
            json+="${sep}{\"module\":\"$m\",\"image_name\":\"$m\"}"
            sep=","
          done
          json+=']}'
          echo "matrix=$json" >> "$GITHUB_OUTPUT"

  ship-containers:
    name: Build and ship
    needs: detect-changes
    if: ${{ needs.detect-changes.outputs.matrix_empty != 'true' }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix: ${{ fromJSON(needs.detect-changes.outputs.matrix) }}
    
    env:
      IMAGE_BASE: europe-west3-docker.pkg.dev/zetta-online-project/ha-gke-docker
      PLATFORMS: linux/amd64,linux/arm64
      DOCKERFILE: ./${{ matrix.module }}/Dockerfile
      # CONTEXT: ./${{ matrix.module }}
      CONTEXT: .

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup QEMU
        uses: docker/setup-qemu-action@v3

      - name: Setup Buildx
        uses: docker/setup-buildx-action@v3

      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: ${{ secrets.GCP_WORKLOAD_IDENTITY_PROVIDER }}
          service_account: ${{ secrets.GCP_SERVICE_ACCOUNT }}


      - name: Setup gcloud CLI
        uses: google-github-actions/setup-gcloud@v2

      - name: Configure Docker for GCP
        run: gcloud auth configure-docker europe-west3-docker.pkg.dev --quiet

      - name: Build & Push
        shell: bash
        run: |
          IMAGE_ID="${{ env.IMAGE_BASE }}/${{ matrix.image_name }}"
          TAGS="latest,${{ github.sha }}"
          
          IFS=',' read -ra TAG_LIST <<< "$TAGS"
          TAG_ARGS=()
          for tag in "${TAG_LIST[@]}"; do
            TAG_ARGS+=(--tag "${IMAGE_ID}:${tag}")
          done

          BA_ARGS=(
            --build-arg "COMMIT_SHA=${{ github.sha }}"
            --build-arg "MODULE=${{ matrix.module }}"
          )

          echo "Building $IMAGE_ID with tags: ${TAG_LIST[*]}"
          
          docker buildx build \
            --platform "${{ env.PLATFORMS }}" \
            -f "${{ env.DOCKERFILE }}" \
            "${BA_ARGS[@]}" \
            "${TAG_ARGS[@]}" \
            --push \
            "${{ env.CONTEXT }}"

  update-manifests:
    name: Update K8s Manifests
    needs: [detect-changes, ship-containers]
    if: ${{ needs.detect-changes.outputs.matrix != '{"include":[]}' && needs.detect-changes.outputs.matrix != '{"include":[{"module":"NONE","image_name":"NONE"}]}' }}
    runs-on: ubuntu-latest
    permissions:
      contents: write
    
    env:
      IMAGE_BASE: europe-west3-docker.pkg.dev/zetta-online-project/ha-gke-docker

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Configure Git
        run: |
          git config --global user.name "GitHub Actions"
          git config --global user.email "actions@github.com"

      - name: Update YAMLs
        shell: bash
        run: |
          echo '${{ needs.detect-changes.outputs.matrix }}' > matrix.json

          jq -c '.include[]' matrix.json | while read i; do
            module=$(echo $i | jq -r '.module')
            image_name=$(echo $i | jq -r '.image_name')
            
            echo "Processing update for: $module"
            
            short_name=${module%-service} 
            deployment_file="k8s/${short_name}-deployment.yaml"
            
            if [[ ! -f "$deployment_file" ]]; then
              echo "⚠️  Warning: Could not find $deployment_file. Trying exact match..."
              deployment_file="k8s/${module}-deployment.yaml"
            fi
            
            if [[ -f "$deployment_file" ]]; then
              echo "✅ Found $deployment_file. Updating image..."
            
              NEW_IMAGE="${{ env.IMAGE_BASE }}/${image_name}:${{ github.sha }}"
              
              sed -i "s|image: .*${image_name}:.*|image: ${NEW_IMAGE}|g" "$deployment_file"
              
              git add "$deployment_file"
            else
              echo "Error: Could not find a deployment file for $module inside k8s/ folder."
            fi
          done

      - name: Commit and Push
        run: |
          # Only commit if there are changes
          if git diff --staged --quiet; then
            echo "No changes to commit."
          else
            git commit -m "GitOps: Update image tags to ${{ github.sha }}"
            git push
          fi